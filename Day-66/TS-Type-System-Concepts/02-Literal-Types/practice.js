// union types
let union;
// any value is allow , from string , number , boolean
union = "apple";
union = 2;
union = true;
// literal types
let literal;
// all value is allowed , which is assigned here
literal = 456; // it is allowed
literal = "mango";
literal = true;
literal = ["hi", "hello"];
export {};
